require File.join(File.dirname(__FILE__), '../lib/cello/structure/foldering/create_project.rb')
require File.dirname(__FILE__) + "/spec_helper"

describe Cello::Structure::CreateProject do
  describe "Create project folders with config files and basic structure" do
    it "Creates the folders to the project" do
    end
    it "Add initial files to the project" do
    end
    it "Add config files to the project" do
    end
  end
end
